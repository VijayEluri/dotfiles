# -*- mode: snippet -*-
# name: for (final Map.Entry<K, V> e : m) { ... }
# key: forentry
# group: collections.iterators
# contributor: phua
# --
for (final Map.Entry<${1:K}, ${2:V}> ${3:e} : ${4:m}.entrySet()) {
    final $1 key = $3.getKey();
    final $2 value = $3.getValue();
    $0
}