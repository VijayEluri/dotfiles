# Local configuration

# set folder=~/Mail
# set spoolfile=/var/spool/mail/phua
# set mbox_type=Maildir
# set mbox=+received
# set record=+sent
# set postponed=+postponed

# mailboxes =INBOX

# set sendmail="/usr/bin/sendmail -oi -oem"
# set sendmail=/usr/bin/msmtp

set timeout=300
set mail_check=300
set sort=threads
# set sort_aux=reverse-last-date-received
set mark_old=no
set fast_reply=yes
set include=yes
set forward_quote=yes
set forward_format="FWD: %s"

# IMAP configuration

set realname="USER NAME"
set from="USER NAME <USER@gmail.com>"
set imap_user=USER@gmail.com
# set imap_pass=

set folder=imaps://imap.gmail.com
set spoolfile=+INBOX
# set mbox_type=mbox
# set mbox="+[Gmail]/All Mail"     # [un]set move
# set record="+[Gmail]/Sent Mail"  # [un]set copy
set postponed=+[Gmail]/Drafts      # [un]set postpone
# set trash=+[Gmail]/Trash         # requires [trash_folder_patch]

mailboxes =INBOX

set imap_passive=no
set imap_keepalive=300
set imap_check_subscribed

# SMTP configuration

set smtp_url=smtps://USER\@gmail.com@smtp.gmail.com:465
# set smtp_pass=

set ssl_starttls=yes
set ssl_force_tls=yes

# Multiple IMAP accounts

# account-hook . 'unset preconnect imap_user imap_pass imap_authenticators; set ssl_starttls=ask-yes'
# source 'cat ~/.mutt/accounts/*|'
# switch with cimap://home or cimap://work

# Options

set header_cache=~/.mutt/cache/headers
set message_cachedir=~/.mutt/cache/messages
set certificate_file=~/.mutt/certificates

source ~/.mutt/colors

set alias_file=~/.mutt/aliases
set sort_alias=alias
set reverse_alias=yes
source $alias_file

# set query_command="abook --mutt-query '%s'"
# set query_command="~/.mutt/scripts/mutt-ldap '%s'"

# ignore *
# unignore Date: From: To: CC: Bcc: Subject: User-Agent: X-Mailer:
# hdr_order Date: From: To: CC: Bcc: Subject: User-Agent: X-Mailer:
# set index_format="%{%b %d} %-15.15L [%Z] %s"

# Macros

macro index,pager gi "<change-folder>=INBOX<enter>" "Inbox"
macro index,pager gr "<change-folder>=[Gmail]/Sent<quote-char> Mail<enter>" "Sent"
macro index,pager gs "<change-folder>=[Gmail]/Starred<enter>" "Starred"
macro index,pager gp "<change-folder>=[Gmail]/Drafts<enter>" "Drafts"
macro index,pager ga "<change-folder>=[Gmail]/All<quote-char> Mail<enter>" "All Mail"
