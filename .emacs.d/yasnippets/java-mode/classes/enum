# -*- mode: snippet -*-
# name: public enum ... { ... }
# key: enum
# group: data-types
# contributor: phua
# --
public enum ${1:`(file-name-sans-extension (file-relative-name (buffer-file-name) "."))`} {
    { $2 }

    public $1($3) {
        $0
    }
}