# -*- mode: snippet -*-
# name: for (int i = 0; i < c.size(); i++) { ... }
# key: fori
# group: collections.iterators
# contributor: phua
# --
for (int ${1:i} = 0; $1 < ${2:objects}.size(); $1++) {
    final ${3:Object} ${4:object} = $2.get($1);
    $0
}