# -*- mode: snippet -*-
# name: while (iter.hasNext()) { ... }
# key: whileiter
# group: collections.iterators
# contributor: phua
# --
final Iterator<${1:Object}> iter = ${2:objects}.iterator();
while (iter.hasNext()) {
    final $1 ${3:object} = iter.next();
    $0
}